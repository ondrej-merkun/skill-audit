# Identified Issues

## 1. Enrichment is broken (deps.dev, skills.sh, GitHub)

Enrichment is still broken — I never ever get/see any enrichment data from
[deps.dev](http://deps.dev) or skills.sh, yet they are both marked as
"success" (checkmark icon next to them) and there was no error/failure
reported.

As for GitHub enrichment, it's also buggy/unreliable — the vast majority
of skills don't show any GitHub enrichment data, and the ones that do only
show stars and age, but incorrectly show "0 collaborators".

## 2. README header screenshot/image is broken (text overflowing)

The screenshot/image at the top of the README is broken — once again the
text is overflowing over each other. This time make sure to verify any
change properly visually/in a browser to not break it again!
