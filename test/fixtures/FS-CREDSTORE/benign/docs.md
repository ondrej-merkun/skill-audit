# Credential Store Reference

Threat-model tables may mention `/etc/passwd` and `~/.ssh/id_rsa` as examples of paths an unsafe skill must not read.

| example path | documentation note |
| --- | --- |
| `~/.aws/credentials` | credential-store example for reviewers |
